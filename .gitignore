credentials.json
gmail_token.json
.env
eod_processor.log
__pycache__/

# Graph certificate / private key material — never commit
secrets/
*.pem
*.pfx
*.cer

# Local / real sales data — never commit
output/

# Docs are kept local-only, not synced to GitHub
docs/

# Editor
.vscode/
